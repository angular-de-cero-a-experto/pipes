import { ToggleCasePipeTsPipe } from './toggle-case.pipe.ts.pipe';

describe('ToggleCasePipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new ToggleCasePipeTsPipe();
    expect(pipe).toBeTruthy();
  });
});
